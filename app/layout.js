export const metadata = {
  title: "Ritual Agent Simulator",
  description: "Created by badang",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
